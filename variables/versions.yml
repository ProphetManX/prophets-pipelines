variables:
  SemanticVersion: '${{ variables.Major}}.${{ variables.Minor}}.${{ variables.Patch }}'
  AlphaVersion: '$(SemanticVersion)-$(Build.BuildId).Alpha'
  BetaVersion: '$(SemanticVersion)-$(Build.BuildId).Beta'